apiVersion: resources.resourcelimiter.io/v1beta1
kind: ResourceLimiter
metadata:
  name: resourcelimiter-sample-fixtures-stopped
spec:
  targets: 
  - default
  types:
    limits.cpu: "1.5"
    limits.memory: "1500Mi"
    requests.cpu: "1.2"
    requests.memory: "1200Mi"
  applied: false
